spring:
  application:
    name: season-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:season}?sslmode=${DB_SSL_MODE:disable}
    username: ${DB_USERNAME:season}
    password: ${DB_PASSWORD:season}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
    open-in-view: false
  flyway:
    enabled: true
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
    consumer:
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: ${PORT:8080}

user-service:
  base-url: ${USER_SERVICE_BASE_URL:http://localhost:8083}
combat-service:
  base-url: ${COMBAT_SERVICE_BASE_URL:http://localhost:8084}
item-service:
  base-url: ${ITEM_SERVICE_BASE_URL:http://localhost:8087}
internal-api:
  secret: ${INTERNAL_API_SECRET:local-internal-secret}
